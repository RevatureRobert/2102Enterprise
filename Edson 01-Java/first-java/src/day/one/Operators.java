/*
    packages work with access modifiers to help encapsulate the information
 */

package day.one;

public class Operators {
    /*
    Numerical operations
        +   addition
        -   subtraction
        /   division
        *   multiplication
        %   modulus
        =
        +=
        -=
        /=
        *=
        %=

     Logical operations
        < less than
        > greater tan
        <=
        >=
        ==
        ! not
        !=
        | logical or
        & logical and
        ^ xor
        && (short circuit, doesnt check both if the first one is already false because is gonna fail anyway)
        ||

      ternary
        ? : String s = i < 6 ? "yes" : "no"

    */
}
