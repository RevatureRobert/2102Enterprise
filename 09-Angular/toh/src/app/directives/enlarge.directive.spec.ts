import { ColoredDirective } from './enlarge.directive';

describe('EnlargeDirective', () => {
  it('should create an instance', () => {
    // const directive = new ColoredDirective();
    // expect(directive).toBeTruthy();
  });
});
